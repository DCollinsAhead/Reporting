{
  "name": "jira-dashboard-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy that holds Jira credentials server-side and exposes narrow, read-only endpoints for the Roost-hosted FPT dashboard.",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
